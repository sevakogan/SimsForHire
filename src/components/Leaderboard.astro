---
import type { RaceSession } from '../data/races'

interface Props {
  session: RaceSession
}

const { session } = Astro.props
const hasResults = session.leaderboard.length > 0
---

<div class="mb-12">
  <!-- Session header -->
  <div class="flex flex-wrap items-center gap-4 mb-6">
    <h3 class="font-display text-2xl md:text-3xl font-semibold text-white tracking-tight">{session.name}</h3>
    <div class="flex flex-wrap items-center gap-2">
      <span class="text-xs text-red bg-red/10  px-3 py-1">
        {session.track}
      </span>
      <span class="text-xs text-gray bg-white/5  px-3 py-1">
        {session.game}
      </span>
      <span class="text-xs text-gray bg-white/5  px-3 py-1">
        {session.car}
      </span>
    </div>
  </div>

  {hasResults ? (
    <!-- Leaderboard table -->
    <div class="border border-white/10  overflow-x-auto">
      <table class="leaderboard-table">
        <thead>
          <tr class="bg-bg">
            <th class="w-16">POS</th>
            <th>DRIVER</th>
            <th>BEST LAP</th>
            <th class="hidden sm:table-cell">GAP</th>
            <th class="hidden md:table-cell">SIMULATOR</th>
          </tr>
        </thead>
        <tbody>
          {session.leaderboard.map((entry, i) => (
            <tr class={i % 2 === 0 ? 'bg-surface' : 'bg-bg'}>
              <td>
                <span
                  class={
                    entry.position === 1
                      ? 'pos-badge pos-1'
                      : entry.position <= 3
                        ? 'pos-badge pos-podium'
                        : 'pos-badge pos-default'
                  }
                >
                  {entry.position}
                </span>
              </td>
              <td class="text-white font-medium">{entry.driver}</td>
              <td class="text-white [font-variant-numeric:tabular-nums]">
                {entry.bestLap}
              </td>
              <td class="hidden sm:table-cell text-red [font-variant-numeric:tabular-nums]">
                {entry.position === 1 ? '—' : entry.gapToLeader}
              </td>
              <td class="hidden md:table-cell text-gray">
                {entry.simulator}
              </td>
            </tr>
          ))}
        </tbody>
      </table>
    </div>
  ) : (
    <!-- Empty state -->
    <div class="border border-white/10  py-16 flex flex-col items-center justify-center gap-3">
      <span class="font-display text-2xl font-semibold text-gray">Results Coming Soon</span>
      <span class="text-gray text-sm font-light">
        Leaderboard times will be posted after the event
      </span>
    </div>
  )}
</div>
